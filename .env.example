# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# IMPORTANT: Service role key should NEVER be exposed to the client
# This should only be used in server-side API routes
# DO NOT prefix with NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Authentication Configuration
# Optional: Restrict access to specific email domains
# Set to 'true' to enable email domain restriction
NEXT_PUBLIC_REQUIRE_EMAIL_DOMAIN=false

# Optional: Comma-separated list of allowed email domains (include @ symbol)
# Example: @company.com,@partner.com
NEXT_PUBLIC_ALLOWED_EMAIL_DOMAINS=

# Application Configuration
# These are optional and have defaults in src/lib/config.ts
# Uncomment to override defaults

# Maximum search query length (default: 500)
# MAX_SEARCH_QUERY_LENGTH=500

# Maximum project title length (default: 200)
# MAX_TITLE_LENGTH=200

# Pagination page size (default: 20)
# PAGE_SIZE=20
